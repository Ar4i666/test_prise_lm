# dependencies
/node_modules

# env files (can opt-in for committing if needed)
.env*
!.env.example

# secrets
credentials.json

# local runtime data (bind-mounted on the server, not part of the source)
database.sqlite
public/uploads/*
!public/uploads/.gitkeep

# deployment artifacts
*.zip

# misc
.DS_Store
*.log

# локальный тестовый стенд — не часть продукта, только для разработки
local-test-server.js
local-price-list.json
local-house-coords.json

# Настройка SSH-туннеля для удаленной БД

Так как основная БД `liftmedia` недоступна напрямую извне, вам необходимо пробросить порт через SSH. 

Запустите следующую команду в терминале (или используйте PuTTY / MobaXterm):

```bash
ssh -N -L 3306:127.0.0.1:3306 <user>@<ваша-ip-адрес-сервера>
```

- `-N` означает "не выполнять удаленные команды" (только проброс портов).
- `-L 3306:127.0.0.1:3306` пробрасывает локальный порт 3306 на удаленный 3306. Если у вас локально уже работает MySQL и порт 3306 занят, используйте `3307:127.0.0.1:3306` (не забудьте обновить порт в `.env`).
- Замените `<user>` и `<ваша-ip-адрес-сервера>` на ваши реальные данные.

После запуска этой команды ваше Node.js приложение (Knex) сможет подключаться к удаленной БД по адресу `127.0.0.1` на порту 3306 (или 3307).
